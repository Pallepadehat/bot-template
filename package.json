{
  "name": "bot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun run scripts/build.ts",
    "start": "bun run .syntax/index.js",
    "start:dev": "bun run dev",
    "start:prod": "bun run build && bun run start"
  },
  "devDependencies": {
    "@prisma/client": "^6.5.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "prisma": "^6.5.0"
  }
}
